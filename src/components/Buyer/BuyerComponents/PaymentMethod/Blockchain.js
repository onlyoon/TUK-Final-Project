import classes from './Blockchain.module.css';

const Blockchain = () =>  {
    return (
        <div className={classes.blockchain}>
        <div>블록체인</div>
        <div></div>
        </div>
    );
};

export default Blockchain;